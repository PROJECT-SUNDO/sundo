package org.sundo.wamis.services;

import org.springframework.stereotype.Service;

/**
 * 관측 정보
 */

@Service
public class ObservationDataInfoService {


/*    public ListData<Precipitation> getRFDataList(ObservationDataSearch search, String timeUnit){
        int page = Utils.onlyPositiveNumber(search.getPage(), 1);
        int limit = Utils.onlyPositiveNumber(search.getLimit(), 10);
        int offset = (page - 1) * limit;

        LocalDate sdate = search.getSdate();
        LocalDate edate = search.getEdate();
        String obscd = search.getObscd().trim();

        QObservation observation = QObservation.observation;
        BooleanBuilder andBuilder = new BooleanBuilder();

        *//* 검색 조건 처리 S *//*


    }*/
}
